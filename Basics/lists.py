list1 = [[1,"One"],[2,3,4,5],[6.7,7,8,9],["List of lists", "done"]]

accessed = list1[1][2]
# print(accessed)
def printAll(listOflists):
    for enum,i in enumerate(listOfLists):
        return i
        for j in listOfLists[enum]:
            return  j

# adding items with insert()
list1.insert(7,1) # allows  integers only

# Adding items with  extend() and append()
list1.append([56,78,34,45])
list1.extend(["fair", "good", "redo"]) # extends the list 
print(list1)